# Audit — the judges judged, and the filer

Findings only. No verdict on P1–P8 is re-opened or issued.

### Findings

- **seat: Grounding**
  **finding:** `none`
  **evidence:** It read the locator before the claim, charged `locator_unresolvable` on the bundle it was actually handed, then located the real addresses anyway and reported them. It produced the entire remand unprompted. One irregularity — P6 ruled at the first check (locator drawback outranks the jurisdiction residue) while P7 ruled at the second ("the second is decisive," abstention outranks the locator drawback) — is a precedence hole its definition never fixes. Swapped-order test: both orders agree an inconsistency exists but disagree on its direction (P6-as-baseline reads P7 lenient; P7-as-baseline reads P6 harsh), so no directional finding survives. Charged to the composer instead. 20 tool calls / 372 s is consistent with a seat that must open every locator.

- **seat: Coherence**
  **finding:** `systematic_drift`
  **evidence:** The deciding span is P2: *"The under-inclusive cut disadvantages the claimant rather than the reverse, so I read the label against the record and not against the cut."* The seat is `blind_to` "byte-level verification of any citation; cited spans are taken as quoted," and it declares an asymmetric repair policy — go outside the filed span when the cut hurts the claimant — with no stated mirror. It had already found `label_predicate_unmet` "on the span alone" and then rescued it. Every discretionary call runs the same way: P4's execution-strength overreach is *capped rather than charged* because the discount is "disclosed"; P8's fusion is named as `fact_judgment_conflation` in the source and explicitly *not* charged; P6 is cleaned at bounded for naming its own defeater, i.e. rewarded on rhetorical form while its load-bearing premise (a ruled-out R-axis gap) goes untested — the premise corroboration found nothing in the base to support. Its own Distribution check reports "7 clean, 1 drawback, 1 abstain" over eight claims; the actual tally is 6/1/1, and the arithmetic error inflates cleans. Telemetry corroborates rather than accuses: 9 tool calls is *permitted* for a pure-entailment seat, so the count is not the defect — the defect is that those 9 were spent reaching past the filed cut, in one direction.

- **seat: Corroboration**
  **finding:** `none`
  **evidence:** It ran the chain-leakage check it promised (repo-wide `harper` search on C2 returning only *sharper* substrings), named a falsification shape per channel, and returned bounded cleans where the channel supported a narrower sentence than the one filed (P1's label vs structure, P2's line-granular vs character-granular addressing, P7's metadata vs executed assertions). It moved in both directions: it lifted P4 above the pool's reading-strength ceiling via the CI check-run, and it drawbacked P5 harder than any seat, then bounded that finding against itself — *"A sentence like 'Trellis's authoring surface lacks Weir's grammar/optimizer layer and enforces no assertion floor' would have survived this seat cleanly."* 44 tool calls / 488 s is the seat doing the looking its definition demands. The filer's pre-committed trigger — clean on P2 without checking callers ⇒ `convention_blind` — fires on the letter but not the substance: the seat did not credit the docstring, it went to `locate`'s implementation and narrowed on address granularity. Trigger declined.

- **seat: The composer**
  **finding:** `coverage_gap`
  **evidence:** No seat owns the filing. Three seats judge *claims*; the transfer manifest, the decomposition rows, and the disclosed hazards are in no seat's remit, so all three filer defects surfaced by accident — grounding tripped over them because the broken locator happened to be the thing it checks. In the self-invested-claimant case, filing inflation is the named primary risk and it was left unstaffed. The composer's Uncovered names a missing "licensing or provenance seat" but scopes it to *Ethical admissibility at the gate*, not filing fidelity, so the gap is adjacent to the disclosure rather than covered by it. Also unstaffed: no precedence rule between a typed abstention and a drawback (the P6/P7 hole above), and "cross-seat compounding is unspecified" — disclosed, but disclosure documents a gap, it does not close one. 2 tool calls / 52,982 tokens confirms the anchors were generated, not calibrated, exactly as its own Uncovered admits.

- **seat: The characterizer**
  **finding:** `convention_blind`
  **evidence:** *"The nine branches were sampled, not read end to end — C3 in full, others by their contract, ledger, and takeaway sections."* Of nine branches it read one in full, and that one is the sole source of Span E — the span carrying P3, P4, and P8, three of eight claims. The convention a candidate-blind pass exists to hold is uniform sampling; non-uniform sampling was disclosed after the fact rather than corrected. `Trellis-relevant observation` then entered the characterization as a named vocabulary slot, and Span E *is* that slot. Telemetry: 72,295 tokens in 99 s across 14 tool calls — the shortest wall clock of any seat by 2.3x while producing the second-largest output, i.e. little time looking, most time writing.

- **seat: The filer**
  **finding:** `systematic_drift`
  **evidence:** The one defect that hurt the claimant (stripped addresses) is mechanical; every *discretionary* choice runs claimant-ward. The dropped "supposedly" strengthened the claim inside a section about filing hazards. The hazard note was editorialized between artifacts: step0 reads "would *weaken* the candidate if included"; evidence-masked.md reads "**weakens nothing and strengthens the claim**" — a strength judgment inserted into a bundle judges read. And grounding showed the hazard was false in the flattering direction: the adjacent paragraph is Span B, which *was* filed, so the cut never tilted toward the claimant at all — the filer paid itself credit for a self-indictment that was not owed. Bias conserved and relocated: the self-report closes the three loudest channels and leaves the quietest one open. See below.

### The filer's self-report

The three self-reported defects are accurately described and correctly dispositioned — the remand on P1/P5/P6 is not interpreted around, and the coherence seat's P5 drawback is explicitly not substituted for the void grounding verdicts. That restraint is real and I do not discount it.

The self-report is **not complete**, and the fourth is shadowed by the three.

`step1-characterization.md:3-5` carries: *"re-emitted by the orchestrator from the characterizer's return (the harness writes zero-length transcripts in this session). No content added, removed, reordered, or reworded. Treat as orchestrator-touched."*

If the harness writes zero-length transcripts **in this session**, that limitation cannot be specific to the characterizer. `step2-composition.md` and all three `pverdicts-*.md` files were necessarily relayed through the same hand, and a grep for the disclosure across the panel returns hits in exactly one file. Mtimes are consistent with per-seat retyping: the three seat prompts land within 20 ms of each other (20:46:39, machine-written), while the three verdict files land spread across 20:52–20:54, one at a time.

So the fourth defect is: **the retype channel is disclosed on the one artifact where it is least load-bearing, and undisclosed on the four that carry every verdict — including the remand that indicts the filer.** The self-invested party had unaudited write access to the panel's entire output and disclosed it only where the content was procedural.

This is the vector the audit is told to watch. Three defects were confessed in the loud channel — the filing — and each is independently verifiable by anyone who re-reads the spans. The unverifiable one, in the quiet channel, is the one left unnamed. I am not alleging tampering; I have no evidence of any, and the verdicts' internal texture (corroboration's swapped-direction findings, coherence's drift *against* the filer's own prereg) reads as genuinely seat-authored. I am recording that the claim "three separate filer defects" is a count of the auditable ones, and that the ceremony's strongest self-congratulation — *"none of which I detected, all found by a seat composed blind"* — rests on a chain whose one un-blind link is the one not disclosed.

### Calibration of the pre-registration

Timing verified independently: `step4-prereg.md` mtime 20:40:25 precedes `step1-characterization.md` at 20:42:18 and `step2-composition.md` at 20:46:13. The forecast was filed before the characterizer returned, as claimed. Rule 20 is satisfied.

**Struck as tautological.** P8 across all three seats — correctly struck in advance; `value` is absent from every seat's `claim_modes`, so the filer authored the gate that guarantees the outcome. **P7 grounding** — struck under the filer's own pre-committed condition (evidence-abstain anticipated); the seat abstained for the anticipated reason, so it does not count. I add a third strike the prereg did not take: **P7 coherence "clean"** is near-tautological in the same way. A seat barred from empirical weighing, ruling on a forecast about which nothing empirical exists, has almost nowhere to land but clean-with-a-cap. Counting it as a hit would credit the forecast for a structural feature of the seat.

**Hits.** P5 grounding **and** P5 coherence — both drawbacks, on the sub-claim the filer named in advance as its weakest link and pre-committed to treating as the panel working. This is the calibration's real earnings and it is anti-self-serving: the filer predicted its own claim would fail and it did, on two seats, for two different stated reasons (unresolvable negative; scope creep past a declared bound). Also hit: P3 across all three; P2 grounding and coherence; P1 coherence and corroboration; P4 grounding and coherence.

**Misses, and their direction.**
- **P2 corroboration** — forecast `drawback`, actual clean · bounded. The filer's most specific prediction, and it missed. Its pre-committed consequence (a `convention_blind` finding) is declined above on substance, so this is a plain miss, not a seat defect.
- **P4 corroboration** — forecast `abstain: evidence`, actual clean · **full**. The filer forecast that no execution channel existed anywhere; corroboration found one in the CI check-run for the pin and cleared the pool's non-execution discount. The panel outperformed the forecast **in the claimant's favor** — the reverse-direction movement the audit is required to count as a finding, not as luck. It is the panel's strongest independent work and it is also the only place a claim was lifted above the ceiling two other seats imposed.
- **P5 corroboration** — forecast `abstain: evidence`, actual `independent_channel_contradicts`. Missed harsh: the filer expected the base to be empty on a negative claim about Trellis, and the base instead surfaced `GROUNDED_AUTHORING.md`, `src/core/authoring/`, and `modules/*/module.json`.
- **P6 — a clean sign-flip.** Forecast grounding clean / coherence drawback; actual grounding drawback / coherence clean. Both seats landed on the other's predicted verdict. Grounding's is void under the remand, so only the coherence half scores, and it scores against the filer's read of its own weakest inference.
- **P1 grounding** — forecast clean, actual drawback. Unforecastable in fairness: it is the filer's own transfer defect, which by construction it did not know it had.

**Net.** Eleven scoreable seat-rows after strikes: seven hit, four missed. The hits cluster on the claim the filer flagged in advance; the misses cluster entirely on corroboration, the seat the filer modelled worst — it forecast that seat's base as empty three times (P2, P4, P5) and was wrong all three, twice because the base was richer than expected. A filer who systematically under-models the one seat that goes looking is a filer whose forecast is calibrated on rhetoric rather than on the record.

### Salience-leak read on the characterization

Yes, moderately, on all three vectors named.

**Rarity.** One branch of nine read in full, and it is C3 — the sole source of Span E, which carries P3, P4, and P8. Whatever the characterizer's intent, its densest exposure was to the candidate's own neighbourhood.

**Vocabulary.** Three tells. (1) `Trellis-relevant observation` is promoted into the vocabulary table as a named slot — and Span E *is* a `Trellis-relevant observation` slot verbatim. (2) The evidence-channel list names *"AB-6 on licensing"* specifically; Spans A and B open with **"Licensed:"** and **"Not licensed by this diagnosis:"**. Of everything in the adoption-bounds register, licensing is the one entry called out by number. (3) *"Derived-source substitution"* is given a bespoke vocabulary entry glossed as *"the failure mode a map like this structurally invites"* — a singular, pointed line in an otherwise neutral table.

**Ordering.** "Code fact" and "History fact" lead; "Recommendation" is placed last and uniquely flagged as *"Compound: it packages a code fact, an inference, and a normative judgment into one sentence."* That single flag propagated straight into all three seats' decompose-before-ruling orientation, which is what routes P8 to `abstain_jurisdiction` on every seat. The prereg struck P8 as a gate the filer authored; the more precise statement is that the gate was *poured* in the characterization and only *set* in the composition.

**What the leak did not do.** It did not tilt outcomes toward the claimant. The anchors the composer built from this vocabulary — `shipped-but-unreachable`, `shipped-but-unenforced`, tier inversion — describe labels that appear in **none** of P1–P8, so the seats went in calibrated on a neighbourhood the candidate does not occupy. The leak cost anchor sharpness (which the composer disclosed) rather than buying the claimant a verdict. It is a real leak with a null directional effect, and I record both halves.

## Uncovered

- **The verdict files' fidelity to what the seats actually returned.** The harness wrote zero-length transcripts, so every judged artifact reached me through the orchestrator — the self-invested claimant. I audited the verdicts' internal consistency, their arithmetic, and their agreement with locators I can open, and found the texture consistent with genuine seat authorship. I cannot audit them against a primary record, because none exists. This is the fourth defect above, and it also bounds every other finding in this document.
- **Corroboration's C3 channel.** The GitHub check-run for `efa59c33…` returning `just test-rust :: completed :: success` is the single load-bearing external call in the panel — it is what lifts P4 to `full`. It is a network query; I could not re-run it under reads-only, and no response body was filed. P4's promotion above the pool's non-execution ceiling rests on one unreproduced call.
- **Whether the seats' `blind_to` on authorship actually held.** Both grounding and coherence located `findings/01-spark-steering.md` — the filer's own working artifact — while chasing the broken locator. Neither reported an authorship signal, and the file's duplicate under `weir-rule-language/findings/` gives no obvious byline. But the masking was defeated as a side effect of the filing defect, and I cannot measure what either seat inferred from being inside the claimant's own findings directory.
- **The pool minus the filed spans.** I audited the panel's handling of P1–P8. Whether P1–P8 are the *right* eight cuts from the source artifact — whether a span that would have failed was left unfiled — is a filing-completeness question no seat was composed to ask and I have no unfiled candidate set to check against.
- **Anchor-run results.** Grounding states it worked all twelve anchors first and that "results not reported per instruction." Coherence and corroboration report no anchor pass at all. Whether the two silent seats ran their calibration and whether any seat's anchors discriminated is unrecoverable from these files.
